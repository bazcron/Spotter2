<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="apps.my.spotter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.activities.EditIssues"
            android:label="Spotter...Edit"/>
        <activity
            android:name=".activities.activities.Home"
            android:label="Spotter...Home"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".activities.activities.Splash"
            android:label="SPLASH SCREEN">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.activities.Add"
            android:label="Spotter...Add"/>
        <activity android:name=".activities.activities.Help"
            android:label="Spotter...Help"/>
    </application>

</manifest>